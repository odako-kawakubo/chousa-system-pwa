/**
 * アプリ全体で共有する基本設定。
 * version は利用者へ表示するレビュー版、revision は同一version内の更新判定に使う。
 * HTMLへ番号を直書きしない。
 */
export const appConfig = {
  appName: '調査システムPWA',
  version: '0.1.6.5M',
  revision: 'M-r1',
  mode: 'completion-review'
};
