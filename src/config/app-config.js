/**
 * アプリ全体で共有する基本設定。
 * version は利用者へ表示する確定版、revision は同一version内の更新判定に使う。
 * HTMLへ番号を直書きしない。
 */
export const appConfig = {
  appName: '調査システムPWA',
  version: '0.1.6.5',
  revision: 'r1',
  mode: 'stable'
};
