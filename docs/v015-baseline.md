# v0.15.10 UI基準版

- `app.html` は `chousa-pwa-v0.15.10-startup-performance-fixed` を加工せず、そのまま配置しています。
- 見た目・配置・既存画面を正確な基準として固定するための版です。
- 前回の簡易UIシェルは使用しません。
- この段階では旧業務ロジックも残っています。次段階で、表示を変えないまま機能単位に分離します。
- `index.html` 側のFirebase Microsoft認証確認構成とFirebase Hosting設定は同梱しています。
